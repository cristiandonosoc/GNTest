config("default_include_dirs") {
  include_dirs = [
    "//",
    "//src",
    root_gen_dir,
  ]

  if (target_os == "linux") {
    include_dirs += [
      "/usr/include",
    ]
  }
}

group("default") {
  deps = [
    ":warhol",
  ]
}

group("warhol") {
  deps = [
    "//src:warhol",
  ]
}
