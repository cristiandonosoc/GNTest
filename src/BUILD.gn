source_set("socket") {
  sources = [
    "socket.cc",
    "socket.h",
  ]
}


config("win_socks") {
  libs = [ "Ws2_32.lib" ]
}

executable("socket_main") {
  sources = [
    "socket_main.cc",
  ]
  configs += [ ":win_socks" ]
  deps = [
    ":socket",
  ]
}
